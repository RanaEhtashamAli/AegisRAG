# IMPORTANT: Do not commit real secret values to source control.
# Use sealed-secrets, External Secrets Operator, or Vault Agent to inject these.
# This file is a template showing required keys.
apiVersion: v1
kind: Secret
metadata:
  name: aegisrag-secrets
  namespace: aegisrag
type: Opaque
stringData:
  POSTGRES_USER: "aegisrag"
  POSTGRES_PASSWORD: "CHANGE_ME"
  POSTGRES_DB: "aegisrag"
  SECRET_KEY: "CHANGE_ME_32_CHARS_MINIMUM"
  FIELD_ENCRYPTION_KEY: ""   # Fernet key — leave empty to disable field encryption
  HF_TOKEN: ""               # HuggingFace token for gated vLLM models (optional)
